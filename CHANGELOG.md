# สรุปการแก้ไข CSS — วารสาร Journal of Public Health and Development (AIHD-MU)

**เว็บไซต์:** https://he01.tci-thaijo.org/index.php/AIHD-MU
**แพลตฟอร์ม:** OJS 3.3.0.8 (Open Journal Systems) + Theme Bootstrap 3
**วิธีการ:** เขียน CSS override 1 ไฟล์ โหลดท้ายสุด — **ไม่แก้ HTML, ไม่แก้ JS, ไม่แก้ CSS เดิม แม้แต่ตัวอักษรเดียว**
**จำนวนหน้าที่ได้รับผล:** ทุกหน้าของวารสาร (ทดสอบกับ 19 หน้าหลัก)

---

## 1. ฟอนต์และการอ่าน (Typography)

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **Font Family** | `sans-serif, Helvetica Neue, Helvetica, Arial` — วาง generic `sans-serif` ไว้หน้าสุด ทำให้ฟอนต์ตัวหลังไม่ถูกใช้เลย และ **ไม่มีฟอนต์ไทย** | **Inter** (ละติน) + **Noto Sans Thai** (ไทย) — เบราว์เซอร์เลือกฟอนต์ที่เหมาะกับแต่ละอักขระอัตโนมัติ |
| **ระยะห่างบรรทัด (line-height)** | `1` ตลอดทั้งเว็บ (~15 จุด) — บรรทัดอัดกัน อ่านยาก | `1.65` สำหรับเนื้อหาทั่วไป, `1.25` สำหรับหัวข้อ, `1.8` สำหรับบทคัดย่อ |
| **ขนาดตัวอักษร** | ล็อกด้วย `!important` หลายจุด เช่น `font: 14px/1 sans-serif !important` | ใช้ระบบขนาดเป็นขั้น (type scale): 12 / 14 / 16 / 18 / 19-22 / 23-30 / 26-36 px — หัวข้อใหญ่ปรับตามขนาดจออัตโนมัติ (clamp) |
| **ข้อความ justify** | ฝัง `style="text-align:justify"` 11 จุดในเนื้อหา — เกิดช่องว่างเป็นแถบ (river gap) | เปลี่ยนเป็น `text-align: left` ทุกจุด — อ่านง่ายขึ้น |
| **text-indent** | ฝัง `style="text-indent:30px"` 11 จุด — ย่อหน้าไม่สม่ำเสมอ | ยกเลิกทั้งหมด ใช้ระยะห่างระหว่างย่อหน้าแทน |

---

## 2. ระบบสี (Color System)

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **ธีมสี** | สีหลากหลายไม่เป็นระบบ (midnightblue, #120552, #070808, #a1a1a1 ฯลฯ) | **ขาว / ดำ / ส้ม (#FF5B0A)** — สีเดียวเป็นจุดเน้น สะอาดตา |
| **ตัวแปรสี (CSS Variables)** | ไม่มี — สีกระจายอยู่ทั่วไฟล์ แก้ทีต้องหาทุกจุด | สีทุกตัวเป็นตัวแปร (`--c-accent`, `--c-ink`, `--c-border` ฯลฯ) — แก้ที่เดียวเปลี่ยนทั้งเว็บ |
| **สีตัวหนังสือในเนื้อหา** | ฝัง `style="color:midnightblue"` 11 จุด | Override เป็นสีดำ/เทาเข้มตามธีมใหม่ |
| **Contrast (WCAG)** | หัว sidebar `#333` บนพื้น `#a1a1a1`, breadcrumb ขาวบนส้ม (3.11:1 ไม่ผ่าน AA) | ทุกคู่สีผ่าน WCAG AA — ตัวอักษรบนพื้นส้มใช้สีดำ #111 (6.07:1), ลิงก์สีส้มบนขาวใช้ #C24400 (5.10:1) |

---

## 3. Header / แถบเมนู (Navbar)

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **ความสูง navbar** | `height: 221.1px` — hardcode ตายตัว | `height: auto` — ขยายตามเนื้อหาอัตโนมัติ |
| **padding เมนู** | `padding-top: 81px` — ดันเมนูลงมาเพื่อเว้นที่ให้โลโก้ | padding ปกติ 12px — ใช้ Flexbox จัดวางแทน |
| **โลโก้** | `height: 150px` — hardcode | responsive: `max-height: 104px` บน desktop, `76px` บน mobile — ปรับตามจอ |
| **ช่องค้นหา** | `margin-top: 58px` — magic number ลอยผิดที่เมื่อย่อจอ | `margin-top: 0` — จัดด้วย Flexbox `margin-left: auto` ดันไปขวา |
| **Layout** | ใช้ float — เมนูทับโลโก้เมื่อย่อจอ | ใช้ **Flexbox** — โลโก้ + เมนู + ช่องค้นหา จัดเรียงอัตโนมัติทุกขนาดจอ |
| **ปุ่ม hamburger** | แสดงที่ < 768px (เมนู 9 รายการ ยัดไม่ลงจอ tablet) | แสดงที่ **< 992px** — tablet ก็ใช้เมนูพับได้ |
| **Dropdown "Other"** | สไตล์พื้นฐาน | กล่องขาว มุมมน เงานุ่ม hover สีส้มอ่อน |

---

## 4. Layout / สัดส่วนคอลัมน์

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **Desktop (>=992px)** | เนื้อหา 66.7% / sidebar 33.3% | เนื้อหา **72%** / sidebar **28%** — คืนพื้นที่ให้เนื้อหามากขึ้น |
| **Tablet (768-991px)** | เนื้อหา 83.3% / sidebar **16.7% (~128px)** — sidebar แคบจนรูปล้น | เนื้อหา **100%** / sidebar **100%** — sidebar เลื่อนลงล่าง จัดเป็น **grid 2 คอลัมน์** |
| **Mobile (<768px)** | เรียงลง 1 คอลัมน์ (ใช้ได้) | เหมือนเดิม + เพิ่มระยะห่างระหว่างเนื้อหากับ sidebar |
| **Container alignment** | Header ใช้ `.container-fluid` (เต็มจอ) แต่เนื้อหาใช้ `.container` (max 1170px) — ไม่ตรงแนวกัน | **ทุกส่วนใช้ max-width เดียวกัน** (1200px) + padding เท่ากัน — โลโก้ เมนู เนื้อหา footer ตรงแนว |

---

## 5. Sidebar

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **กรอบ block** | พื้นเทา หัว block พื้น `#a1a1a1` | การ์ดขาว กรอบบาง (`1px solid #E5E5E5`) มุมมน |
| **หัว block (.title)** | พื้นเทา ตัวหนังสือเข้ม | ตัวหนังสือดำ + **เส้นใต้ส้ม 2px** (ไม่มีพื้นเทา) |
| **ปุ่ม Make a Submission** | กล่องขาวธรรมดา (บั๊กจาก specificity) | **ปุ่มส้มเต็มความกว้าง** ตัวอักษรดำ |
| **โลโก้ index (Scopus/TCI/Crossref ฯลฯ)** | เรียงลงเป็นแถวยาวมาก | จัดเป็น **grid อัตโนมัติ** เรียงเป็นแถวสวยงาม |
| **รูป badge width="200"/"300"** | ล้นออกนอก sidebar | `max-width: 100%` — ย่อตามกรอบอัตโนมัติ |

---

## 6. Footer

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **พื้นหลัง** | `#eee` (เทาอ่อน) | **สีดำ** (`#111`) — ตัดกับเนื้อหาขาวชัดเจน |
| **ตัวอักษร** | สีเข้มบนเทา, line-height 1 | สีเทาอ่อน `#D8D8D8` บนดำ (contrast 18.88:1), line-height 1.65 |
| **ลิงก์** | สีพื้นฐาน | สีขาว มี underline, hover เปลี่ยนเป็นส้ม |

---

## 7. Page Header (แถบหัวข้อ)

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **สไตล์** | พื้นส้มทึบ `#FF5B0A` + padding 1px — ชื่อบทความยาวกลายเป็นก้อนส้มเต็มจอ | พื้นโปร่ง + **เส้นซ้ายส้มหนา 4px** + หัวข้อดำ — สะอาด รองรับข้อความยาว |
| **Breadcrumb** | พื้นส้ม + ตัวปัจจุบันสีขาวบนส้ม (ไม่ผ่าน contrast) | พื้นโปร่ง ตัวหนังสือเทา ตัวปัจจุบันดำตัวหนา ตัวคั่น `/` สีอ่อน |

---

## 8. หน้าแรก (Index)

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **กล่องแนะนำวารสาร** | ตารางธรรมดา ไม่มีกรอบ | **การ์ดขาว** มีกรอบ มุมมน เส้นบนสีส้ม |
| **ตาราง `<table width="1035">`** | ล้นจอมือถือ (ต้นเหตุหลักของ horizontal scroll) | `width: 100%`, mobile แตกเป็น block เรียงลง |
| **ประกาศ (Announcements)** | รายการธรรมดา | **การ์ดมีกรอบ** hover มีเงา |
| **ฉบับปัจจุบัน (Current Issue)** | สไตล์พื้นฐาน | มีไอคอน หัวข้อชัดเจน ปุ่มดูเพิ่มมีลูกศร |

---

## 9. หน้าบทความ (Article View) — หน้าสำคัญที่สุด

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **DOI** | จางบนพื้นส้มจนแทบมองไม่เห็น | เป็น meta line สีเทาบนพื้นขาว อ่านชัด |
| **ปุ่มดาวน์โหลด PDF** | ปุ่มเล็ก | **ปุ่มส้มเด่นเต็มความกว้าง** ตัวอักษรดำ ขนาดใหญ่ กดง่าย |
| **บทคัดย่อ (Abstract)** | ไม่จำกัดความกว้าง, line-height 1 | `max-width: 72ch` + `line-height: 1.8` — อ่านสบายตา |
| **References** | เรียงติดกัน | hanging indent + ระยะห่างระหว่างรายการ |
| **Keywords** | ข้อความธรรมดา | **chip** (ป้ายมุมมน พื้นเทาอ่อน) |
| **Citations (Scopus/Crossref/Google)** | เรียงลง | เรียงเป็น **แถว badge** แนวนอน |
| **Mobile** | ต้องเลื่อนผ่าน abstract ยาวก่อนเห็นปุ่ม PDF | **ปก + ปุ่ม PDF ขึ้นก่อน** abstract (ใช้ CSS `order`) |

---

## 10. หน้า Archive (คลังฉบับย้อนหลัง)

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **Layout** | รายการแนวยาว (media list) | **grid ปก** `repeat(auto-fill, minmax(200px, 1fr))` — แสดงปกเป็นตารางสวยงาม |
| **ปุ่ม pagination** | เล็ก กดยากบนมือถือ | ขนาดขั้นต่ำ 44x44px กดง่าย |
| **ป้าย Current** | ข้อความธรรมดา | **badge ส้ม** มุมมนเต็ม (pill) |

---

## 11. หน้าค้นหา (Search)

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **แถบค้นหา** | ช่อง input + ปุ่มธรรมดา | **การ์ดค้นหา** มีกรอบ มุมมน พื้นเทาอ่อน |
| **Mobile** | input + ปุ่มอยู่แถวเดียว (บีบกัน) | แยกเป็น **2 แถว** (input เต็มบรรทัด + ปุ่มเต็มบรรทัด) |
| **ผลค้นหา** | รายการธรรมดา | มีเส้นคั่นระหว่างรายการ อ่านง่าย |

---

## 12. หน้าเกี่ยวกับ (About / Editorial Team / Submissions ฯลฯ)

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **inline style 49 จุด** | `color:midnightblue`, `font-family:Arial`, `font-size:130%`, `text-align:justify` | Override ทั้งหมดให้เข้าธีมใหม่ — สีเดียวกัน ฟอนต์เดียวกัน |
| **ตาราง editorial team** | `<table width="704">` + `<td width="300">` x86 ช่อง — ล้นจอมือถือ | Desktop: `table-layout: fixed; width: 100%` / Mobile: แตกเป็น card list (`td { display: block }`) |
| **เนื้อหายาว** | ไม่จำกัดความกว้าง — บรรทัดยาวเกินอ่านสบาย | `max-width: 78ch` — จำกัดความกว้างบรรทัดให้อ่านง่าย |
| **ลำดับหัวข้อ (heading hierarchy)** | ขนาดไม่สม่ำเสมอ | h2 / h3 มีขนาดชัดเจน + spacing สม่ำเสมอ |

---

## 13. Responsive / รองรับทุกขนาดจอ

| ปัญหา | สาเหตุ | วิธีแก้ |
|---|---|---|
| **มือถือ 390px: เนื้อหาล้นขอบขวา** | `<table width="1035">` ในหน้าแรก | ตาราง → `width: 100%`, mobile แตกเป็น block |
| **มือถือ: padding กินพื้นที่** | gutter 30px ซ้าย-ขวา = 60px จากจอ 390px | ลดเป็น **16px** ซ้าย-ขวา (เพิ่มพื้นที่ 28px) |
| **Tablet 768-991px: sidebar 128px แต่รูปกว้าง 200-300px** | `col-sm-2` + `img width="200"` | sidebar 100% + grid 2 คอลัมน์ + `img { max-width: 100% }` |
| **เมนูทับโลโก้เมื่อย่อจอ** | `padding-top: 81px` + โลโก้ 150px | Flexbox layout + padding ปกติ |
| **ช่องค้นหาลอยผิดที่** | `margin-top: 58px` | `margin-top: 0` + Flexbox จัดวาง |
| **ตาราง editorial team ล้นจอ** | `table width="704"` + `td width="300"` x86 | Mobile: `display: block` / Tablet: scroll แนวนอนในกรอบ |
| **บรรทัดอัดกันทั้งเว็บ** | `line-height: 1` ~15 จุด | `line-height: 1.65` |
| **ปุ่ม/ลิงก์เล็กเกินแตะ** | ไม่มีการกำหนด min size | `min-height: 44px` บนเมนู ปุ่ม pager ทุกตัว |

---

## 14. Accessibility (การเข้าถึง)

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **Focus indicator** | เส้นขอบเบราว์เซอร์เริ่มต้น (มักมองไม่เห็น) | **เส้นขอบส้ม 3px** + offset 2px — เห็นชัดเจน |
| **Touch target** | ปุ่ม/ลิงก์หลายตัวเล็กกว่า 44px | ทุก interactive element ขนาดขั้นต่ำ **44x44px** |
| **Color contrast** | บางคู่ไม่ผ่าน WCAG AA | **ทุกคู่สีผ่าน WCAG AA** (ตัวปกติ >= 4.5:1 / ตัวใหญ่ >= 3:1) |
| **Skip to content** | มี แต่ซ่อนแล้วไม่แสดงเมื่อ focus | แสดงเมื่อ focus — พื้นดำ ตัวขาว |
| **Reduced motion** | ไม่รองรับ | `prefers-reduced-motion: reduce` → ปิด transition/animation ทั้งหมด |
| **Print** | พิมพ์ได้ทุกส่วนรวม header/sidebar | ซ่อน header, sidebar, footer — พิมพ์แต่เนื้อหาเต็มหน้า |

---

## 15. Components ทั่วไป

| หัวข้อ | ของเดิม | แก้ไขเป็น |
|---|---|---|
| **ปุ่ม Primary** | หลายสีไม่สม่ำเสมอ, มี text-shadow, gradient | **ส้มเรียบ** ไม่มี gradient ไม่มี text-shadow, มุมมน 8px, hover มีเงา |
| **ปุ่ม Default** | เทาธรรมดา | กรอบเทา พื้นขาว hover เปลี่ยนเป็นดำ |
| **Form input** | สไตล์ Bootstrap พื้นฐาน | กรอบเทา focus เป็นส้ม + เงาส้มอ่อน |
| **Panel** | พื้นเหลือง `#FFFBCF` | พื้นขาว กรอบเทา หัว panel พื้นเทาอ่อน |
| **Card ทุกใบ** | ไม่มีเงา/มุมมน | กรอบบาง มุมมน 10px เงาเบา hover มีเงาเพิ่ม |
| **Galley link (ดาวน์โหลด)** | ลิงก์ธรรมดา | **ปุ่ม** พื้นส้มอ่อน กรอบส้ม hover เป็นส้มทึบ |
| **Selection (drag เลือกข้อความ)** | สีเริ่มต้นของเบราว์เซอร์ | พื้นส้ม ตัวขาว — เข้ากับธีม |

---

## 16. ตัวเลขสรุป

| รายการ | จำนวน |
|---|---|
| ไฟล์ที่แก้ไข | **1 ไฟล์** (`css/override.css`) |
| ไฟล์ HTML ที่แก้ | **0 ไฟล์** |
| ไฟล์ JS ที่แก้ | **0 ไฟล์** |
| ขนาด CSS | ~5,300 บรรทัด / ~170 KB |
| CSS Variables (ตัวแปร) | ~40 ตัว (สี ฟอนต์ ระยะ เงา) |
| หน้าที่ทดสอบ | 19 หน้า x 3 ขนาดจอ = **57 เคส** |
| Horizontal overflow (เนื้อหาล้นจอ) | **0 เคส** (จากเดิมล้นหลายหน้า) |
| CSS Parse Error | **0** |

---

## 17. สิ่งที่ยังรอตัดสินใจ

| รายการ | รายละเอียด |
|---|---|
| **`html { filter: saturate(80%) }`** | ThaiJO ยัดโค้ดนี้ทุกหน้า ทำให้สีทั้งเว็บซีดลง 20% (เป็นการไว้อาลัย) — ตอนนี้ยังคงไว้ตามเดิม มีโค้ดเตรียมปลดไว้แล้ว (1 บรรทัด) รอตัดสินใจ |
| **ทดสอบเบราว์เซอร์จริง** | ทดสอบด้วย Chromium — ยังไม่ได้ทดสอบบน Safari iOS / Firefox / Edge จริง |

---

## 18. วิธี Deploy ขึ้น Production

ไม่ต้องแก้ไขอะไรบน server — อัปโหลดผ่านหน้า admin ของ OJS:

**Settings > Website > Appearance > Journal Style Sheet**

> **หมายเหตุ:** ช่องนี้คือที่เดียวกับที่ `styleSheet.css` เดิมอยู่ แนะนำให้เอาไฟล์เดิมต่อท้ายด้วย override.css แล้วอัปโหลดเป็นไฟล์เดียว (วิธี Append) เพื่อให้ผลลัพธ์ตรงกับที่ทดสอบ
